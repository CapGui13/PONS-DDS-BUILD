--- mb04/pons-critic.js
+++ mb05/pons-critic.js
@@ -11011,6 +11011,16 @@
     const legal=(c)=>!ctx.isLegal||ctx.isLegal(history,c,seat);
     const out=(c,reason,convention='sef2024-reference',extra={})=>legal(c)?{call:c,reason,semantic:{natural:!['X','XX'].includes(c),source:'sef2024-benchmark-v03-reference',forcing:'unknown',publishWhenNative:true,convention,...extra}}:null;
 
+    // R7 FIXED MB05 — strong delayed four-card Heart fit after opener's direct 3H.
+    if(raw==='4H' && relHistory.length===6 && relHistory[0]?.call==='1H' && relHistory[1]?.call==='PASS' &&
+       ['1S','2C','2D'].includes(relHistory[2]?.call) && relHistory[3]?.call==='PASS' && relHistory[4]?.call==='3H' && relHistory[5]?.call==='PASS'){
+      const hld4=chailleyFitHld(ctx.deal,seat,'H',5), ownAces=SUITS.reduce((n,su)=>n+(String(hand[su]||'').includes('A')?1:0),0), ownKeys=ownAces+(String(hand.H||'').includes('K')?1:0);
+      if((L.H||0)===4 && hld4>=16 && ownKeys>=2 && legal('4NT')) return {
+        call:'4NT',changed:true,reason:`R7 FIXED MB05 : fit Coeur 4e différé + 3C, ${hld4} HLD et ${ownKeys} clés => RKCB`,
+        semantic:{natural:false,source:'r7-fixed-mb05-delayed-heart-fit-rkcb-after-opener-3H',forcing:'one_round_if_uncontested',publishWhenNative:true,convention:'blackwood-5keys-30-41',trump:'H',semanticRole:'rkcb-ask',artificialType:'rkcb-ask'}
+      };
+    }
+
     // R7 FIXED MB04 — strong delayed four-card Spade fit after opener's direct 3S.
     if(raw==='4S' && relHistory.length===6 && relHistory[0]?.call==='1S' && relHistory[1]?.call==='PASS' &&
        ['2C','2D'].includes(relHistory[2]?.call) && relHistory[3]?.call==='PASS' && relHistory[4]?.call==='3S' && relHistory[5]?.call==='PASS'){
