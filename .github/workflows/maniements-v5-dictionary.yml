name: MANIEMENTS V5 — Dictionary V2 backfill (PAUSED)

# V2 generated exact probabilities but only a first-defense witness.
# Production backfill is intentionally paused while the V3 multi-trick
# bridge-plan extractor is qualified. Frozen solver/runtime semantics are untouched.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dictionary V2 backfill intentionally paused pending V3 extractor qualification."
